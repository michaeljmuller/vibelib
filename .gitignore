# secrets — src/docker/.env holds live API keys and passwords
src/docker/.env
.env

# actual books and library data — never commit
*.epub
*.m4b
data/

# The loader and resolver were CLI tools that never lived on this branch. That
# work is now the /ingest page (src/python/web/web/ingest). What did not come
# across -- the audit sweep and the acquisition-date matcher -- is still in the
# tag, which is the place to look if either is ever wanted again:
#
#     git show cli-tooling-2026-07-21:src/python/resolver/resolver/audit.py

# local tool state
.claude/settings.local.json
.DS_Store

# python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/
build/
dist/
